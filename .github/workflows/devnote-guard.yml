name: devnote-guard

on:
  push:
  pull_request:

jobs:
  devnote-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: node tools/validate-devnotes.mjs --strict

